server:
  port: 8082

spring:
  application:
    name: seating-service

  datasource:
    url: jdbc:postgresql://localhost:5435/seating_db
    username: seating_user
    password: seating_password
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-init-sql: SET TIME ZONE 'UTC'

  jpa:
    hibernate:
      ddl-auto: create-drop   # Auto-create tables for tests
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
